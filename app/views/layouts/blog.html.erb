<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title><%= @page_title %></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'blogs', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <div class="container">
      <%= render 'shared/blog_nav' %>
      

      <div class="jumbotron p-3 p-md-5 text-white rounded bg-dark">
        <div class="col-md-6 px-0">
          <h1 class="display-4 font-italic"><%= Blog.last.title %></h1>
          <p class="lead my-3"><%= truncate(Blog.last.body, :length => 150)  %></p>
          <p class="lead mb-0"><a href="../../blogs/<%=a= Blog.last.slug %>" class="text-white font-weight-bold">Continue lendo...</a></p>
        </div>
      </div>

     <%= link_to 'Novo Post', new_blog_path if logged_in?(:site_admin) %>
    <div class="container">
      <div class ="row">
        <%= yield %>
        <%= render 'shared/blog_sidebar' %>
        <%= source_helper("blog") %>

        <%= render 'shared/blog_footer' %>
      </div>
    </div>
  </body>
</html>
